generated: '2026-08-04'
method: generated
source: openapi/onpay-api-openapi.json + https://onpay.readme.io
note: >-
  OnPay publishes no AGENTS.md, skill files, or agent guidance — the ReadMe hub's own llms.txt
  enumerates every page it has and there is nothing agent-facing. These skills are generated by API
  Evangelist from the published OpenAPI 2.0.0 and the authorization/versioning guides. Because the
  spec declares no operationId on any of its 58 operations, each step is grounded in METHOD + path
  verified against openapi/onpay-api-openapi.json.
skills:
- file: onpay-onboard-an-employee.md
  name: Onboard an employee in OnPay
  api: openapi/onpay-api-openapi.json
  operations:
  - POST /employees
  - PUT /employees/{employee_id}/bank-accounts
  - PATCH /employees/{employee_id}/wages
  - PATCH /employees/{employee_id}/tax-fields
  - PATCH /employees/{employee_id}/tax-fields/worksite
  - GET /employees/{employee_id}
  - GET /company/worksites
  - GET /company/departments
  - GET /company/positions
- file: onpay-change-employee-pay-safely.md
  name: Change employee pay safely in OnPay
  api: openapi/onpay-api-openapi.json
  operations:
  - GET /employees/{employee_id}/wages
  - PATCH /employees/{employee_id}/wages
  - POST /employees/{employee_id}/wages/schedule
  - GET /employees/{employee_id}/wages/schedule
  - GET /employees/{employee_id}/deductions
  - PATCH /employees/{employee_id}/deductions/{deduction_id}
  - POST /employees/{employee_id}/deductions/{deduction_id}/schedule
  - GET /employees/{employee_id}/leave
  - PATCH /employees/{employee_id}/leave
- file: onpay-reconcile-a-pay-run.md
  name: Reconcile an OnPay pay run
  api: openapi/onpay-api-openapi.json
  operations:
  - GET /reports/approved-pay-runs
  - GET /reports/listing
  - GET /reports/pagination/listing
  - GET /reports/retirement-summary
  - GET /company/pay-schedules
  - GET /company/pay-schedule-dates
